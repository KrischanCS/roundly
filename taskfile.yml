version: '3'

shopt: [ -s globstar ]

tasks:
  test:
    cmds:
      - go test ./...
    sources:
      - ./**/*.go
      - taskfile.yml

  check:
    cmds:
      - go tool govulncheck ./...
      - go tool golangci-ling fmt
      - go tool golangci-lint run --output.tab.path stdout --output.tab.print-linter-name --output.tab.colors
      - task test
    sources:
      - ./**/*.go
      - taskfile.yml
      - .golangci.yml

  deps:
    cmds:
      - go get -u ./...
      - go mod tidy
      - go mod vendor
    sources:
      - ./**/*.go
      - go.mod
      - taskfile.yml
      - .golangci.yml
